import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  overflow: {
    borderRadius: 10,
    overflow: 'hidden',
    height: 70,
  },
  resize: {
    resizeMode: 'cover',
    height: 70,
    width: '100%',
  },
});
